<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Provider Creation Request | Plutto</title>
  
  <!-- Favicon (use .ico or .png as desired) -->
  <link rel="icon" href="/images/plutto-icon.png" type="image/png">
  
  <!-- Google Fonts (Nunito Sans) -->
  <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@400;600;700&display=swap" rel="stylesheet">
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <!-- Font Awesome Icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" rel="stylesheet">
  
  <style>
    body {
      background-color: #00072D; /* Approx. dark navy from getplutto.com */
      font-family: 'Nunito Sans', sans-serif;
      color: #ffffff; /* White text */
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin-top: 50px;
      margin-bottom: 50px;
      background-color: #ffffff; /* White background for the form */
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.3);
      color: #000; /* Dark text inside the form */
    }
    .plutto-logo {
      display: block;
      margin: 0 auto 20px auto;
      width: 120px; 
      height: auto; /* Maintains aspect ratio */
    }
    .header-text {
      color: #00072D; /* Tie the heading color to the brand color, or use black #000 */
      text-align: center;
      margin-bottom: 20px;
    }
    .form-label {
      font-weight: 600; /* Slightly bolder text */
    }
    .btn-primary {
      background-color: #00072D; /* Match brand color if you prefer a dark button */
      border-color: #00072D;
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Transparent Plutto Logo -->
    <img 
      src="/images/plutto-logo.png" 
      alt="Plutto Logo" 
      class="plutto-logo"
    >
    <h2 class="header-text">
      <i class="fas fa-building"></i> Create a Provider
    </h2>
    <!-- Just an example snippet -->
<form action="/submit" method="POST">
    <div class="mb-3">
      <label for="accessKey" class="form-label">Access Key</label>
      <input type="text" id="accessKey" name="accessKey" class="form-control" required>
    </div>
  
    <div class="mb-3">
      <label for="providerTin" class="form-label">TIN (Tax ID)</label>
      <input type="text" id="providerTin" name="providerTin" class="form-control" required>
    </div>
  
    <div class="mb-3">
      <label for="providerName" class="form-label">Provider Name</label>
      <input type="text" id="providerName" name="providerName" class="form-control" required>
    </div>
  
    <div class="mb-3">
      <label for="providerEmail" class="form-label">Provider Email (optional)</label>
      <input type="email" id="providerEmail" name="providerEmail" class="form-control">
    </div>
  
    <div class="mb-3">
      <label for="providerDetails" class="form-label">Details (optional)</label>
      <textarea id="providerDetails" name="providerDetails" class="form-control"></textarea>
    </div>
  
    <button type="submit" class="btn btn-primary w-100">
      Submit Request
    </button>
  </form>
  </div>
  
  <!-- Bootstrap Bundle JS (includes Popper) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>