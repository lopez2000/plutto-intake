<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Validation Request Submitted | Plutto</title>
  
  <!-- Favicon -->
  <link rel="icon" href="/images/plutto-icon.png" type="image/png">

  <!-- Google Fonts (Nunito Sans) -->
  <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@400;600;700&display=swap" rel="stylesheet">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Font Awesome Icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" rel="stylesheet">

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/form-style.css">
</head>
<body>
  <div class="container">
    <img 
      src="/images/plutto-logo.png"
      alt="Plutto Logo"
      class="plutto-logo"
    >
    <h2 class="header-text">
      <i class="fas fa-check-circle text-success"></i> Validation Request Submitted
    </h2>

    <p>Your validation request has been submitted successfully. We'll notify you once it's complete.</p>

    <% if (providerName) { %>
      <p>Provider Name: <strong><%= providerName %></strong></p>
    <% } %>
    <% if (providerTin) { %>
      <p>TIN: <strong><%= providerTin %></strong></p>
    <% } %>

    <a href="/" class="btn btn-primary w-100">
      <i class="fas fa-arrow-left"></i> Back to Home
    </a>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>